import React from 'react'

const HomeSection = ({header}) => {
  return (
    <div>
        <h2 className='text-white text-8xl pb-8 text-center'>{header}</h2>
      </div>
  )
}

export default HomeSection
